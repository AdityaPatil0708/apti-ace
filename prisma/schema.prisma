generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User{
  id        Int     @default(autoincrement())
  fullname  String  
  email     String  @unique
  password  String
}